server:
  port: 8081
spring:
  application:
    name: auth-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
trainmark:
  auth:
    store: ${TRAINMARK_AUTH_STORE:memory}
    jdbc:
      url: ${TRAINMARK_AUTH_JDBC_URL:}
      username: ${TRAINMARK_AUTH_JDBC_USERNAME:}
      password: ${TRAINMARK_AUTH_JDBC_PASSWORD:}
