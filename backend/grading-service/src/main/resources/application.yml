server:
  port: 8085
spring:
  application:
    name: grading-service
management:
  endpoints:
    web:
      exposure:
        include: health,info

trainmark:
  scoring:
    provider: ${SCORING_PROVIDER:local}
    command: ${SCORING_COMMAND:}
    timeout-seconds: ${SCORING_TIMEOUT_SECONDS:90}
