server:
  port: 8086
spring:
  application:
    name: ocr-service
management:
  endpoints:
    web:
      exposure:
        include: health,info

trainmark:
  ocr:
    provider: ${OCR_PROVIDER:local}
    command: ${OCR_COMMAND:}
    timeout-seconds: ${OCR_TIMEOUT_SECONDS:60}
